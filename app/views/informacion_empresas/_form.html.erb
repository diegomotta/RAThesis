<%= form_for ([@empresa, @informacion_empresa])  do |f| %>

  <% if @informacion_empresa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@informacion_empresa.errors.count, "error") %> prohibited this informacion_empresa from being saved:</h2>

      <ul>
      <% @informacion_empresa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <form class="form-horizontal" >
      <fieldset>
        <legend><h3>Datos de Noticias</h3></legend>
          <div class="form-group">
            <label for="" class="col-lg-4 control-label">Título de Item:</label>
            <div class="col-lg-12">
              <%= f.text_field :itemTitulo, class: "form-control", placeholder: "Título"%>
            </div>
          </div>
          <div class="form-group">
              <div class="col-lg-12 text-center">
                  <%= f.submit "Guardar", class: "btn btn-info btn-raised" %>
                  <%= link_to 'Volver', empresa_informacion_empresas_path,class: "btn btn-info btn-raised" %>
              </div>
          </div>
      <% end %>
      </fieldset>
  </form>
