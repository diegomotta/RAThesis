<%= form_for ([@empresa,@producto])  do |f| %>
  <% if @producto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@producto.errors.count, "error") %> prohibited this producto from being saved:</h2>

      <ul>
      <% @producto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<form class="form-horizontal" >
  <fieldset>
    <legend>Datos de Categoria del Producto</legend>
     <div class="form-group">
      <label for="" class="col-lg-2 control-label">Titulo:</label>
      <div class="col-lg-10">
        <%= f.text_field :titulo, class: "form-control", placeholder: "Título"%>
      </div>
    </div>
  <div class="col-lg-offset-4">
    <div class="btn-group">
      <a> <%= f.submit "Guardar", class: "btn btn-info btn-raised" %></a>
    </div>
    <div class="btn-group">     
     <a> <%= link_to 'Volver', empresa_productos_path,class: "btn btn-info btn-raised" %></a>
   </div>
  </div>  
<% end %>
