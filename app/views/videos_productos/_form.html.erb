<%= form_for([@empresa,@producto,@item_producto,@videos_producto]) do |f| %>
  <% if @videos_producto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@videos_producto.errors.count, "error") %> prohibited this item_producto from being saved:</h2>

      <ul>
      <% @videos_producto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <form class="form-horizontal" >
      <fieldset>
        <legend>Datos de Video del Producto</legend>
          <div class="form-group">
            <label for="" class="col-lg-4 control-label">Título:</label>
            <div class="col-lg-12">
              <%= f.text_field :titulo, class: "form-control", placeholder: "Titulo"%>
            </div>
          </div>
          <div class="form-group">
            <label for="" class="col-lg-4 control-label">Descripción:</label>
            <div class="col-lg-12">
              <%= f.text_area :descripcion, class: "form-control", placeholder: "Descripción"%>
            </div>
          </div>
          
          <div class="form-group">
            <label for="" class="col-lg-4 control-label">Dirección URL:</label>
            <div class="col-lg-12">
              <%= f.text_field :url, class: "form-control", placeholder: "Dirección URL"%>
            </div>
          </div>



          <div class="form-group">
              <div class="col-lg-12 text-center">
                  <%= f.submit "Guardar", class: "btn btn-info btn-raised" %>
                  <%= link_to 'Atrás', empresa_producto_item_producto_videos_productos_path,class: "btn btn-info btn-raised" %>
              </div>
          </div>
</fieldset>
</form>
<% end %>
