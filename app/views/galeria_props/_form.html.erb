<%= form_for([@empresa,@producto,@prop_general,@galeria_prop]) do |f| %>
  <% if @galeria_prop.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@galeria_prop.errors.count, "error") %> prohibited this galeria_prop from being saved:</h2>

      <ul>
      <% @galeria_prop.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <form class="form-horizontal" >
    <fieldset>
        <legend>Datos de la Imagen de Información Nutricional</legend>
        <div class="form-group">
          <label for="" class="col-lg-2 control-label">Imagen:</label>
          <div class="col-lg-10">
              <%= f.file_field :image, class: "form-control"%>
          </div>
        </div>
        <div class="form-group">
          <div class="col-lg-12 text-center">
            <%= f.submit "Guardar", class: "btn btn-info btn-raised" %>
            <%= link_to 'Atrás', empresa_producto_prop_general_galeria_props_path,class: "btn btn-info btn-raised" %>
          </div>
        </div>
    </fieldset>
<% end %>
