<%= form_for([@empresa,@producto,@prop_general]) do |f| %>
  <% if @prop_general.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@prop_general.errors.count, "error") %> prohibited this prop_general from being saved:</h2>

      <ul>
      <% @prop_general.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripción %><br>
    <%= f.text_area :descripción %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
  
<% end %>
