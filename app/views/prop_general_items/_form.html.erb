<%= form_for([@empresa,@producto,@prop_general,@prop_general_item]) do |f| %>
<% if @prop_general_item.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@prop_general_item.errors.count, "error") %> prohibited this prop_general_item from being saved:</h2>

  <ul>
    <% @prop_general_item.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>
<form class="form-horizontal" >
  <fieldset>
  <legend>Datos de Item de Información Nutricional</legend>
    <div class="form-group">
      <label for="" class="col-lg-2 control-label">Item:</label>
      <div class="col-lg-12">
        <%= f.text_field :item, class: "form-control", placeholder: "Item"%>
      </div>
    </div>
    <div class="form-group">
      <label for="" class="col-lg-2 control-label">Descripción:</label>
      <div class="col-lg-12">
        <%= f.text_area :descripción, class: "form-control", placeholder: "Descripción"%>
      </div>          
    </div>
    <div class="form-group">
      <div class="col-lg-12 text-center">
        <%= f.submit "Guardar", class: "btn btn-info btn-raised" %>
        <%= link_to 'Atrás', empresa_producto_prop_general_prop_general_items_path, class: "btn btn-info btn-raised" %>
      </div>
    </div>
  </fieldset>
</form>
<% end %>
